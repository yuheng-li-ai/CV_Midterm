name: resnet18_freeze_then_unfreeze_extension
model: resnet18
attention: none
pretrained: true
num_classes: 37
data_root: data
download: false
image_size: 224
batch_size: 32
num_workers: 4
val_ratio: 0.15
epochs: 20
freeze_backbone_epochs: 3
backbone_lr: 0.00005
head_lr: 0.001
weight_decay: 0.0001
label_smoothing: 0.0
seed: 42
device: cuda:2
log_backend: csv
run_dir: runs/classification/resnet18_freeze_then_unfreeze_extension
checkpoint_dir: checkpoints/classification/resnet18_freeze_then_unfreeze_extension
