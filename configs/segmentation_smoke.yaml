name: unet_cpu_smoke
num_classes: 3
data_root: data
download: true
image_size: 64
batch_size: 2
num_workers: 0
val_ratio: 0.15
epochs: 1
max_train_batches: 2
max_eval_batches: 1
base_channels: 8
loss: combo
lr: 0.001
weight_decay: 0.0001
dice_smooth: 1.0
seed: 42
device: cpu
log_backend: csv
run_dir: runs/segmentation/unet_cpu_smoke
checkpoint_dir: checkpoints/segmentation/unet_cpu_smoke
save_vis: 2
